import React from "react";

const ExcelIcon = props => {
    return(
        <div>
            <img className="img-fluid icon" id ="excel-icon" alt="Excel" src={require('../../Images/Excel-icon.png')}/>
        </div>
    )
}

export default ExcelIcon;